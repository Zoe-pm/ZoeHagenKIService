Zielverhalten (UX)
	1.	Sofort sichtbar: Beim Laden der Startseite ist das Video als Standbild (Poster/erster Frame) sichtbar.
	2.	Verzögertes Autoplay mit Ton: Exakt 2 Sekunden nach Page-Load startet das Video mit Ton automatisch.
	3.	Einfaches Steuern: Unten rechts im Video ist eine dezente Pause/Play-Schaltfläche (keine Browser-Overlays).
	•	Klick auf Pause: Video stoppt, Bild bleibt auf dem aktuellen Frame stehen.
	•	Klick auf Play: Video läuft an genau dieser Stelle weiter.
	4.	Ton-Schaltfläche (optional, klein): Falls du eine Ton-Schaltfläche vorsiehst, gilt:
	•	Wird der Ton erst später eingeschaltet (z. B. weil beim Start stumm gespielt werden musste), dann springt das Video automatisch an den Anfang (0:00) und startet mit Ton neu, damit der/die Nutzer:in den gesamten Text/Intro versteht.
	•	Wichtig: Beim regulären Autoplay mit Ton (wie unter 2) gibt es keinen ungewollten Sprung – nur dann zurück zum Anfang, wenn Ton nachträglich aktiviert wird.
	5.	Kein störender Overlay-Play-Button des Browsers (eigene Controls verwenden, Standard-Controls ausblenden).
	6.	Ende-Verhalten: Wenn das Video zu Ende ist, springt es zurück auf den Startframe (Poster/Frame 0) und bleibt dort stehen. Ein erneuter Klick auf Play startet es wieder von vorne mit Ton.
	7.	Barrierearm & mobil: Funktioniert auf Desktop, iOS und Android. Der Play/Pause-Button ist mit Tastatur fokussierbar und hat aussagekräftige ARIA-Labels.
	8.	Design: Buttons dezent (halbtransparent, abgerundete Ecken), unten rechts gestapelt, damit das Bild nicht verdeckt wird.

Technische Anforderungen & Fallbacks
	•	Autoplay mit Ton ist von manchen Browsern (insb. iOS/Safari) blockiert. Implementiere daher:
a) Primärpfad: Versuche nach 2 s Autoplay mit Ton.
b) Fallback: Falls blockiert, starte nach 2 s stumm. Sobald die Nutzer:in irgendeine Interaktion ausführt (z. B. Klick auf Play/Ton-Button), springe auf 0:00 und spiele mit Ton von Anfang an.
	•	Kein „Mittensprung“ beim Ton an/aus:
	•	Ton-Wechsel während der ersten 2 s (vor Start) → ignoriere und halte das Zielverhalten bei.
	•	Ton-Wechsel nach Start:
	•	Wenn der Start stumm war (Fallback), dann beim Ton-Einschalten → Restart ab 0:00 mit Ton.
	•	Wenn der Start mit Ton gelang, dann kein Sprung – die Stelle bleibt erhalten.
	•	Ende-Rücksprung: Beim Event „ended“ immer auf 0:00 zurück und pausieren, damit der Startframe sichtbar ist.
	•	Keine Browser-Overlays/Controls: Verwende eigene Buttons; Standard-Controls aus.

Positionierung & Interaktion
	•	Buttons:
	•	Play/Pause: unten rechts im Video, Hauptsteuerung.
	•	Ton an/aus (falls vorhanden): direkt darüber, kleiner, gleiches Styling.
	•	Zustände & Labels:
	•	Play/Pause-Button wechselt Label/Ikon (▶︎/⏸︎) + aria-label („Video abspielen/pausieren“).
	•	Ton-Button wechselt Label/Ikon (🔊/🔇) + aria-label („Ton einschalten/ausschalten“).
	•	Kein Vollbild-Zwang auf iOS: Inline-Wiedergabe sicherstellen (playsinline-Verhalten).

Abnahme-Checkliste (bitte Punkt für Punkt testen)
	•	Beim Laden der Seite ist das Video als Startbild sichtbar.
	•	Nach 2 s versucht das Video mit Ton zu starten.
	•	Wenn der Browser Ton-Autoplay blockiert, startet es stumm; erste Interaktion (Play/Ton) → Restart ab 0:00 mit Ton.
	•	Pause/Play funktioniert, Button unten rechts, keine Browser-Overlays.
	•	Ton nachträglich aktivieren (nur wenn stumm gestartet): Video springt auf 0:00 und spielt mit Ton von vorne.
	•	Wenn das Video endet, springt es sofort auf den Startframe zurück.
	•	Erneutes Abspielen von vorn ist möglich, Ton ist an.
	•	Keine ungewollten Sprünge während normaler Wiedergabe.
	•	Funktioniert auf Chrome/Edge/Firefox (Desktop) und Safari/Chrome (iOS/Android).
	•	Buttons sind tastaturbedienbar und haben sinnvolle ARIA-Labels.

Übergaben/Assets
	•	Ein Poster-Bild (Startframe) bereitstellen, damit der erste Eindruck sofort hochwertig ist.
	•	Sicherstellen, dass das Video web-tauglich ist (MP4/H.264, sinnvolle Bitrate).